const songTimer = 15;
const gamePoints = songTimer * 100;
const songNumber = 5;
const numberOfAnswers = 4;

const gameConfig = {
  gamePoints,
  songTimer,
  songNumber,
  numberOfAnswers,
};

export default gameConfig;
